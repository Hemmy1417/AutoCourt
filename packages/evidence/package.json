{
  "name": "@autocourt/evidence",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@autocourt/shared-types": "*"
  },
  "//": "Resolves to COMPILED output so plain Node (apps/worker) can load it; Next transpiles the same dist. Run `npx tsc -b` before building or running anything that consumes it."
}