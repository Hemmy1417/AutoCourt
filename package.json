{
  "name": "autocourt",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "genlayer-js": "^2.0.0-rc.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
